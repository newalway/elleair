{% extends "FOSUserBundle::bundle-layout.html.twig" %}

{% trans_default_domain 'FOSUserBundle' %}

{% block title %}{{ 'registration.check_email_title'|trans }} {{ parent() }}{% endblock %}

{% block content %}
<section class="bg-default section-md">
<div class="container fos-container text-center">
    <h1 class="text-center">{{ 'registration.check_email_title'|trans }}</h1>

    <div class="row">
        <div class="fos-regis-check-email">
            <i class="ion-paper-airplane" style="font-size:3em;"></i>
            <p style="margin-top:30px;">
                {{ 'registration.check_email'|trans({'%email%': user.email}) }}
            </p>
            {# <div class="text-center">
            <a href="{{path('fos_user_security_login')}}" class="btn btn-default arrow-push-out-right"><span>{% trans %}layout.login{% endtrans %} <i class="fa fa-caret-right"></i></span></a>
            </div> #}
        </div>
    </div>
</div>
</section>
{% endblock content %}
